# 습관 생성 AI Agent 프로젝트 구조 제안서

## 📋 문서 정보
- **프로젝트명**: 습관 생성 AI Agent
- **버전**: 1.0
- **작성일**: 2026-01-XX

---

## 1. 프로젝트 구조 개요

본 문서는 습관 생성 AI Agent 프로젝트의 폴더 구조 및 파일 조직 방식을 제안합니다.

---

## 2. 루트 디렉토리 구조

```
uHavit/
├── .github/                 # GitHub 설정
│   └── workflows/          # CI/CD 워크플로우
├── docs/                    # 프로젝트 문서
│   ├── 01-프로젝트-요건정의서.md
│   ├── 02-기능-명세서.md
│   ├── 03-기술-명세서.md
│   ├── 04-MVP-범위-정의서.md
│   └── 05-프로젝트-구조-제안서.md
├── public/                  # 정적 파일
│   ├── favicon.ico
│   └── images/             # 공용 이미지
├── src/                     # 소스 코드
│   ├── components/         # React 컴포넌트
│   ├── pages/              # 페이지 컴포넌트
│   ├── hooks/              # Custom Hooks (React Query)
│   ├── services/           # API 서비스
│   ├── utils/              # 유틸리티 함수
│   ├── constants/          # 상수 정의
│   ├── styles/             # 전역 스타일
│   ├── App.jsx             # 루트 컴포넌트
│   └── main.jsx            # 진입점
├── supabase/               # Supabase 설정
│   ├── functions/          # Edge Functions
│   │   └── ai-chat/       # AI 채팅 함수
│   ├── migrations/        # 데이터베이스 마이그레이션
│   └── config.toml         # Supabase 설정
├── .env.local              # 환경 변수 (로컬)
├── .env.example            # 환경 변수 예시
├── .gitignore
├── .npmrc                   # pnpm 설정 (선택)
├── index.html
├── package.json
├── pnpm-lock.yaml          # pnpm 잠금 파일
├── vite.config.js
└── README.md
```

---

## 3. src 디렉토리 상세 구조

### 3.1 components/
```
src/components/
├── common/                 # 공통 컴포넌트
│   ├── Button/
│   │   ├── Button.jsx
│   │   ├── Button.module.css
│   │   └── index.js
│   ├── Input/
│   ├── Modal/
│   ├── Loading/
│   └── ErrorBoundary/
├── habits/                 # 습관 관련 컴포넌트
│   ├── HabitList/
│   │   ├── HabitList.jsx
│   │   ├── HabitCard.jsx
│   │   └── index.js
│   ├── HabitForm/
│   ├── HabitDetail/
│   └── HabitCheckin/
├── chat/                   # AI 채팅 컴포넌트
│   ├── ChatWindow/
│   │   ├── ChatWindow.jsx
│   │   ├── MessageBubble.jsx
│   │   └── index.js
│   ├── ChatInput/
│   └── QuickActions/
├── stats/                  # 통계 컴포넌트
│   ├── Dashboard/
│   ├── StatsChart/
│   ├── CalendarHeatmap/
│   └── StreakCounter/
└── layout/                 # 레이아웃 컴포넌트
    ├── Header/
    ├── Sidebar/
    ├── Footer/
    └── Layout.jsx
```

### 3.2 pages/
```
src/pages/
├── Auth/
│   ├── Login.jsx
│   ├── SignUp.jsx
│   └── ResetPassword.jsx
├── Dashboard.jsx           # 대시보드 페이지
├── Habits/
│   ├── HabitList.jsx
│   ├── HabitDetail.jsx
│   └── HabitCreate.jsx
├── Chat.jsx                # AI 채팅 페이지
├── Stats.jsx               # 통계 페이지
└── Settings.jsx            # 설정 페이지
```

### 3.3 hooks/
```
src/hooks/
├── useAuth.js             # 인증 관련 Hook
├── useHabits.js           # 습관 관리 Hook (React Query)
├── useCheckins.js         # 체크인 관리 Hook (React Query)
├── useAIChat.js           # AI 채팅 Hook
├── useStats.js            # 통계 계산 Hook
├── useNotifications.js    # 알림 Hook
└── useLocalStorage.js    # 로컬 스토리지 Hook
```

### 3.4 services/
```
src/services/
├── supabase/
│   ├── client.js          # Supabase 클라이언트
│   ├── auth.js            # 인증 서비스
│   ├── habits.js          # 습관 API
│   ├── checkins.js        # 체크인 API
│   └── conversations.js   # 대화 기록 API
├── openai/
│   └── chat.js            # OpenAI API 래퍼
└── storage/
    ├── indexedDB.js       # IndexedDB 관리
    └── sync.js            # 동기화 로직
```

### 3.5 utils/
```
src/utils/
├── date.js                # 날짜 유틸리티
│   ├── formatDate()
│   ├── getDateRange()
│   └── isToday()
├── stats.js               # 통계 계산
│   ├── calculateSuccessRate()
│   ├── calculateStreak()
│   └── getPatternAnalysis()
└── validation.js          # 유효성 검사
    ├── validateHabit()
    └── validateCheckin()
```

### 3.6 constants/
```
src/constants/
└── index.js               # 상수 정의
    ├── FREQUENCY_TYPES
    ├── CATEGORIES
    └── 기타 상수들
```

### 3.8 styles/
```
src/styles/
├── variables.css          # CSS 변수
├── reset.css              # CSS 리셋
├── globals.css            # 전역 스타일
└── themes/
    ├── light.css
    └── dark.css
```

---

## 4. 주요 파일 예시

### 4.1 컴포넌트 구조 예시

#### HabitCard 컴포넌트
```
src/components/habits/HabitList/HabitCard.jsx
```

```javascript
// components/habits/HabitList/HabitCard.jsx
import { useCheckinMutation } from '@/hooks/useCheckins';
import styles from './HabitCard.module.css';

/**
 * 습관 카드 컴포넌트
 * @param {Object} props
 * @param {Object} props.habit - 습관 데이터
 */
export function HabitCard({ habit }) {
  const { mutate: toggleCheckin } = useCheckinMutation();
  
  return (
    <div className={styles.card}>
      <h3>{habit.name}</h3>
      <p>연속 {habit.streak}일</p>
      <button onClick={() => toggleCheckin({ habitId: habit.id })}>
        체크인
      </button>
    </div>
  );
}
```

### 4.2 Hook 예시

#### useHabits Hook
```
src/hooks/useHabits.js
```

```javascript
// hooks/useHabits.js
import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query';
import { getHabits, createHabit, updateHabit, deleteHabit } from '@/services/supabase/habits';

/**
 * 습관 목록 조회 Hook
 */
export function useHabits() {
  return useQuery({
    queryKey: ['habits'],
    queryFn: getHabits,
    staleTime: 1000 * 60 * 5, // 5분
  });
}

/**
 * 습관 생성 Mutation Hook
 */
export function useCreateHabit() {
  const queryClient = useQueryClient();
  
  return useMutation({
    mutationFn: createHabit,
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['habits'] });
    },
  });
}

/**
 * 습관 수정 Mutation Hook
 */
export function useUpdateHabit() {
  const queryClient = useQueryClient();
  
  return useMutation({
    mutationFn: ({ id, updates }) => updateHabit(id, updates),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['habits'] });
    },
  });
}

/**
 * 습관 삭제 Mutation Hook
 */
export function useDeleteHabit() {
  const queryClient = useQueryClient();
  
  return useMutation({
    mutationFn: deleteHabit,
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['habits'] });
    },
  });
}
```

### 4.3 서비스 예시

#### habits.js 서비스
```
src/services/supabase/habits.js
```

```javascript
// services/supabase/habits.js
import { supabase } from './client';

/**
 * 습관 목록 조회
 * @returns {Promise<Array>} 습관 배열
 */
export async function getHabits() {
  const { data, error } = await supabase
    .from('habits')
    .select('*')
    .eq('is_active', true)
    .order('created_at', { ascending: false });
  
  if (error) throw error;
  return data;
}

/**
 * 습관 생성
 * @param {Object} habit - 습관 데이터
 * @returns {Promise<Object>} 생성된 습관
 */
export async function createHabit(habit) {
  const { data, error } = await supabase
    .from('habits')
    .insert(habit)
    .select()
    .single();
  
  if (error) throw error;
  return data;
}

/**
 * 습관 수정
 * @param {string} id - 습관 ID
 * @param {Object} updates - 수정할 데이터
 * @returns {Promise<Object>} 수정된 습관
 */
export async function updateHabit(id, updates) {
  const { data, error } = await supabase
    .from('habits')
    .update(updates)
    .eq('id', id)
    .select()
    .single();
  
  if (error) throw error;
  return data;
}

/**
 * 습관 삭제
 * @param {string} id - 습관 ID
 * @returns {Promise<void>}
 */
export async function deleteHabit(id) {
  const { error } = await supabase
    .from('habits')
    .delete()
    .eq('id', id);
  
  if (error) throw error;
}
```

### 4.4 상수 정의 예시

#### constants/index.js
```
src/constants/index.js
```

```javascript
// constants/index.js

/**
 * 빈도 타입 상수
 */
export const FREQUENCY_TYPES = {
  DAILY: 'daily',
  WEEKLY: 'weekly',
  MONTHLY: 'monthly'
};

/**
 * 카테고리 상수
 */
export const CATEGORIES = {
  EXERCISE: 'exercise',
  READING: 'reading',
  HEALTH: 'health',
  LEARNING: 'learning',
  WORK: 'work',
  OTHER: 'other'
};

/**
 * 최대 습관 이름 길이
 */
export const MAX_HABIT_NAME_LENGTH = 50;

/**
 * 최대 설명 길이
 */
export const MAX_DESCRIPTION_LENGTH = 200;
```

---

## 5. 네이밍 컨벤션

### 5.1 파일명
- **컴포넌트**: PascalCase (예: `HabitCard.jsx`)
- **Hook**: camelCase with `use` prefix (예: `useHabits.js`)
- **유틸리티**: camelCase (예: `dateUtils.js`)
- **상수**: camelCase (예: `constants.js`)
- **스타일**: `ComponentName.module.css` 또는 `component-name.css`

### 5.2 변수명
- **컴포넌트**: PascalCase (예: `HabitCard`)
- **함수/변수**: camelCase (예: `getHabits`, `habitList`)
- **상수**: UPPER_SNAKE_CASE (예: `MAX_HABITS`)
- **JSDoc 타입**: JSDoc 주석으로 타입 정보 제공

### 5.3 폴더명
- **컴포넌트 폴더**: PascalCase (예: `HabitList/`)
- **기타 폴더**: kebab-case (예: `habit-list/`) 또는 camelCase (예: `habitList/`)

---

## 6. import 경로 별칭 (Alias)

### 6.1 vite.config.js 설정
```javascript
// vite.config.js
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';
import path from 'path';

export default defineConfig({
  plugins: [react()],
  resolve: {
    alias: {
      '@': path.resolve(process.cwd(), './src'),
      '@components': path.resolve(process.cwd(), './src/components'),
      '@pages': path.resolve(process.cwd(), './src/pages'),
      '@hooks': path.resolve(process.cwd(), './src/hooks'),
      '@services': path.resolve(process.cwd(), './src/services'),
      '@utils': path.resolve(process.cwd(), './src/utils'),
      '@constants': path.resolve(process.cwd(), './src/constants'),
    }
  }
});
```

### 6.2 사용 예시
```javascript
// 절대 경로 사용
import { HabitCard } from '@components/habits/HabitList';
import { useHabits } from '@hooks/useHabits';
import { CATEGORIES } from '@constants';
```

---

## 7. 코드 조직 원칙

### 7.1 컴포넌트 분리 원칙
- **단일 책임**: 각 컴포넌트는 하나의 책임만 가짐
- **재사용성**: 공통 컴포넌트는 `common/`에 배치
- **도메인별 그룹화**: 관련 컴포넌트는 같은 폴더에 배치

### 7.2 상태 관리 원칙
- **로컬 상태**: `useState` 사용
- **서버 상태**: React Query 사용 (데이터 fetching, 캐싱, 동기화)
- **전역 UI 상태**: Context API 또는 React Query의 전역 상태 활용

### 7.3 파일 구조 원칙
- **컴포넌트 폴더**: 컴포넌트 파일, 스타일 파일, index.js 포함
- **Barrel Export**: `index.js`로 재export하여 import 간소화

---

## 8. 환경 변수 관리

### 8.1 .env.example
```bash
# Supabase
VITE_SUPABASE_URL=your_supabase_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# OpenAI (서버에서만 사용)
OPENAI_API_KEY=your_openai_api_key
```

### 8.2 환경 변수 사용
```javascript
// services/supabase/client.js
import { createClient } from '@supabase/supabase-js';

export const supabase = createClient(
  import.meta.env.VITE_SUPABASE_URL,
  import.meta.env.VITE_SUPABASE_ANON_KEY
);
```

---

## 9. 빌드 및 배포 구조

### 9.1 빌드 산출물
```
dist/
├── index.html
├── assets/
│   ├── index-[hash].js
│   ├── index-[hash].css
│   └── images/
└── favicon.ico
```

### 9.2 배포 구조
- **프론트엔드**: Vercel에 배포
- **백엔드**: Supabase (자동 배포)
- **Edge Functions**: Supabase Functions에 배포

---

## 10. 문서화 구조

### 10.1 코드 문서화
- **JSDoc 주석**: 복잡한 함수에 설명 및 타입 정보 추가
- **함수 설명**: 모든 public 함수에 JSDoc 주석 추가
- **README**: 각 주요 폴더에 README.md 추가 (선택)

### 10.2 예시
```javascript
/**
 * 습관의 성공률을 계산합니다.
 * 
 * @param {Array} checkins - 체크인 기록 배열
 * @param {number} totalDays - 전체 일수
 * @returns {number} 성공률 (0-100)
 */
export function calculateSuccessRate(checkins, totalDays) {
  const completedCount = checkins.filter(c => c.is_completed).length;
  return Math.round((completedCount / totalDays) * 100);
}
```

---

## 부록

### A. 패키지 관리자

이 프로젝트는 **pnpm**을 사용합니다.

#### pnpm 설치
```bash
npm install -g pnpm
```

#### 주요 명령어
```bash
# 의존성 설치
pnpm install

# 개발 서버 실행
pnpm dev

# 프로덕션 빌드
pnpm build

# 빌드 미리보기
pnpm preview

# 린트 검사
pnpm lint
```

#### pnpm 사용 이유
- 디스크 공간 효율성 (심볼릭 링크 사용)
- 빠른 설치 속도
- 엄격한 의존성 관리
- 모노레포 지원

#### .npmrc 설정 (선택)
```ini
# .npmrc
shamefully-hoist=false
strict-peer-dependencies=true
auto-install-peers=true
```

#### package.json engines 필드
```json
{
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "packageManager": "pnpm@8.0.0"
}
```

### B. 권장 VS Code 확장
- ESLint
- Prettier
- JavaScript (ES6) code snippets
- Tailwind CSS IntelliSense (사용 시)

### C. 권장 설정 파일

#### .prettierrc
```json
{
  "semi": true,
  "singleQuote": true,
  "tabWidth": 2,
  "trailingComma": "es5"
}
```

#### .eslintrc.json
```json
{
  "extends": [
    "eslint:recommended",
    "plugin:react/recommended",
    "plugin:react/jsx-runtime"
  ],
  "parserOptions": {
    "ecmaVersion": 2022,
    "sourceType": "module",
    "ecmaFeatures": {
      "jsx": true
    }
  },
  "rules": {
    "react/react-in-jsx-scope": "off"
  }
}
```

---

이 구조는 MVP 개발을 위한 기본 구조이며, 프로젝트 진행에 따라 조정될 수 있습니다.
